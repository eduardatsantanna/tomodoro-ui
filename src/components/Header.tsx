import React from 'react';

const Header: React.FC = () => {
  return (
    <header style={{ padding: '1rem', textAlign: 'center', border: '3px solid #a67c52' }}>
      <h1>Pomodoro Timer</h1>
    </header>
  );
};

export default Header;
